name: Compile

on: 
  push:
    branches:
      - main
      - development

jobs:
  install-packages:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt install -y autotools-dev gcc make
      - run: gcc --version  

