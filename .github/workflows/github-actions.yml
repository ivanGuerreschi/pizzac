name: Compile

on: 
  push:
    branches:
      - main
      - development

jobs:
  install-packages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: sudo apt-get install -y autotools-dev gcc make
      - run: autoreconf --install
      - run: ./configure
      - run: make  

